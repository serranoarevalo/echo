//Imports

//Actions

//ActionCreators

//API Actions

//Initial State
const initialState = {
  isLoggedIn: true
};

//Reducer
function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

//Reducer Functions

//Default Reducer Export
export default reducer;
